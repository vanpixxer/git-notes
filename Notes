GIT INIT - initializes git repo
GIT ADD . - add files to staging area
GIT COMMIT - M "Initial Commit" - commit

To see if you have git insalled:   git --version

To open integrated Terminal in Visual Studio Code use:   control ~

To see all commits:  git log

To create a new repo on the CLI:
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/vanpixxer/projectName.git
git push -u origin master

To push an existing repo from the command line:
git remote add origin https://github.com/vanpixxer/projectName.git
git push -u origin master


fatal: Could not read from remote repository. Please make sure you have the 
correct access rights and the repository exists:
             git remote set-url origin https://github.com/user_name/repo_name.git


To clone a github repo:   git clone https://github.com/user_name/project_name.git


To check remote repo:    git remote -v